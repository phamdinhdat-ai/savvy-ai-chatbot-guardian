
apiVersion: v1
kind: Service
metadata:
  name: savvy-ai-chatbot
  labels:
    app: savvy-ai-chatbot
spec:
  selector:
    app: savvy-ai-chatbot
  ports:
  - port: 80
    targetPort: 8000
  type: ClusterIP
